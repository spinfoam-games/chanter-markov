const MARKOV_MODE = require('./constants').MARKOV_MODE

const MarkovBuilder = require('./MarkovBuilder')
const MarkovGenerator = require('./MarkovGenerator')

module.exports = {
    MARKOV_MODE,

    MarkovBuilder,
    MarkovGenerator,
}